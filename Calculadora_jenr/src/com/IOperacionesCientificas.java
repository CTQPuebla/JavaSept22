package com;

public interface IOperacionesCientificas {
	
	double raizCuadrada (double numero);
	double seno (double numero);
	double potencia (double base, double exponente);
	double coseno (double numero);

}
